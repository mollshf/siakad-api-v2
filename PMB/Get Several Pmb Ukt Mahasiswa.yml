info:
  name: Get Several Pmb Ukt Mahasiswa
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/pmb/ukt"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
