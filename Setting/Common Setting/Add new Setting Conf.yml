info:
  name: Add new Setting Conf
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/setting"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: |-
      {
          "nameofset": "Buka jadwal edit KRS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
