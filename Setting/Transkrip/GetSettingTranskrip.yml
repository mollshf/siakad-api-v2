info:
  name: GetSettingTranskrip
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/setting/transkrip"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
