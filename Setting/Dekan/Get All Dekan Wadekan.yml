info:
  name: Get All Dekan Wadekan
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/admin/election/dekan-wadekan"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
