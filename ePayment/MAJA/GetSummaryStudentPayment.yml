info:
  name: GetSummaryStudentPayment
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/maja/summary"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
