info:
  name: GetLeftDonation
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/maja/donation"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
