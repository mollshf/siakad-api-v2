info:
  name: GetStudentTuition
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/maja/tuition"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
      description: "24130002"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
