info:
  name: SyncDataDiriMahasiswa 2
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/neofeeder/v2/sync/data/mahasiswa"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: |-
      [
          2018,
          2017,
          2016,
          2015,
          2014
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
