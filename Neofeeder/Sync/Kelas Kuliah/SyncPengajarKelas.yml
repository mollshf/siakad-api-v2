info:
  name: SyncPengajarKelas
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/neofeeder/v2/sync/pengajar"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
