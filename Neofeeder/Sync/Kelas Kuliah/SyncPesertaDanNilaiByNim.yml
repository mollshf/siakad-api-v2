info:
  name: SyncPesertaDanNilaiByNim
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/neofeeder/v2/sync/peserta-nilai-kelas/nim"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: |-
      {
          "nim": "24130101"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
