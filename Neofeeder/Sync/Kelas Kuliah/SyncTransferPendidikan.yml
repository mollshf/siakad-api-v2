info:
  name: SyncTransferPendidikan
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/api/neofeeder/v2/sync/transfer-pendidikan"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
