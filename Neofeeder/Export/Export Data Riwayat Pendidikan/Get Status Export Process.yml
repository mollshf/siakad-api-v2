info:
  name: Get Status Export Process
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/import/riwayat-pendidikan"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
