info:
  name: GetHistoryExport
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/neofeeder/v2/export/kelas/history"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
