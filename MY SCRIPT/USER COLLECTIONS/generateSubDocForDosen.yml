info:
  name: generateSubDocForDosen
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/test/user/sub/nama"
  body:
    type: json
    data: |-
      {
          "nama": "ZU IRFAN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
