info:
  name: HandleMultipleSubUserDocuments Copy
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/test/tucked/field"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
