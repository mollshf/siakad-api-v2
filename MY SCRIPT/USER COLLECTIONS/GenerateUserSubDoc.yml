info:
  name: GenerateUserSubDoc
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/test/user/sub"
  body:
    type: json
    data: |-
      {
          "username": "0101057601"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
