info:
  name: test Copy 2
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/test/user/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
