info:
  name: HandleMultipleSubUserDocuments Copy 2
  type: http
  seq: 11

http:
  method: GET
  url: "{{url}}/api/test/tucked/clear"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
