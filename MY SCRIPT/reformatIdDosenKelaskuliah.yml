info:
  name: reformatIdDosenKelaskuliah
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/test/reformatKelasKuliah"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
