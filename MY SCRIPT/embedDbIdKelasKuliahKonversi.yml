info:
  name: embedDbIdKelasKuliahKonversi
  type: http
  seq: 19

http:
  method: GET
  url: "{{url}}/api/test/embed/idkelas/konversi-transkrip"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
