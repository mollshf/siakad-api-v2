info:
  name: removeMahasiswaDuplikat
  type: http
  seq: 13

http:
  method: GET
  url: "{{url}}/api/test/mahasiswa"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
