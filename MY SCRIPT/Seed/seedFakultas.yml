info:
  name: seedFakultas
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/test/seed/fakultas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
