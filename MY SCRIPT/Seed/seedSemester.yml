info:
  name: seedSemester
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/test/seed/semester"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
