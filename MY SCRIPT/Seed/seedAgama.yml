info:
  name: seedAgama
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/test/seed/agama"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
