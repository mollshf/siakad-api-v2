info:
  name: seedSugek
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/test/seed/sugek"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
