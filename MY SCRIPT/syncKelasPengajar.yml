info:
  name: syncKelasPengajar
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/test/sync/kelas-pengajar"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
