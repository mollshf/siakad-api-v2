info:
  name: GetTanskripNilai
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/transkrip/:nim"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: "21121005"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
