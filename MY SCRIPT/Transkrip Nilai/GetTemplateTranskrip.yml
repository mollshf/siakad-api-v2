info:
  name: GetTemplateTranskrip
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/transkrip/:nim/template"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: "19150109"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
