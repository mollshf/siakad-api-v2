info:
  name: embedAngaktanMahsiswa
  type: http
  seq: 20

http:
  method: GET
  url: "{{url}}/api/test/embed/angkatan/mahasiswa"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
