info:
  name: GetFakultas
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=fakultas"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: type
      value: fakultas
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
