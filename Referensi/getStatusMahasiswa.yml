info:
  name: getStatusMahasiswa
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=status-mahasiswa"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: type
      value: status-mahasiswa
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
