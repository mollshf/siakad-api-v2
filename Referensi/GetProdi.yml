info:
  name: GetProdi
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=prodi"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: limit
      value: "10"
      type: query
      disabled: true
    - name: offset
      value: "0"
      type: query
      disabled: true
    - name: type
      value: prodi
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
