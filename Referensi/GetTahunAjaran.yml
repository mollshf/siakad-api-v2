info:
  name: GetTahunAjaran
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=tahun-ajaran"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: limit
      value: "10"
      type: query
      disabled: true
    - name: offset
      value: "0"
      type: query
      disabled: true
    - name: type
      value: tahun-ajaran
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
