info:
  name: getPerguruanTinggi
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=perguruan-tinggi&term=021009"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: type
      value: perguruan-tinggi
      type: query
    - name: term
      value: "021009"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
