info:
  name: getJenisPendaftaran
  type: http
  seq: 11

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=jenis-pendaftaran"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: type
      value: jenis-pendaftaran
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
