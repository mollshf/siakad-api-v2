info:
  name: GetJenisEvaluasi
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=jenis-evaluasi"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: type
      value: jenis-evaluasi
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
