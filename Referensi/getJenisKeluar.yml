info:
  name: getJenisKeluar
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=jenis-keluar"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: type
      value: jenis-keluar
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
