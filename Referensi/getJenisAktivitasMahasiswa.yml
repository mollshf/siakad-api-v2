info:
  name: getJenisAktivitasMahasiswa
  type: http
  seq: 12

http:
  method: GET
  url: "{{url}}/api/perkuliahan/referensi?type=jenis-aktivitas-mahasiswa"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: type
      value: jenis-aktivitas-mahasiswa
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
