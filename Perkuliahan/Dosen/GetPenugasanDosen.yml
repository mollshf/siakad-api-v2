info:
  name: GetPenugasanDosen
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/perkuliahan/dosen/penugasan?term=um"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: offset
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "2"
      type: query
      disabled: true
    - name: term
      value: um
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
