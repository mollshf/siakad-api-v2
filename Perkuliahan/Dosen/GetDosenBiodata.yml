info:
  name: GetDosenBiodata
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/user/biodata/dosen"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
