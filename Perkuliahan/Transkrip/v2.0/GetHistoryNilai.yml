info:
  name: GetHistoryNilai
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/transkrip/:nim/history"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: "20182012"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
