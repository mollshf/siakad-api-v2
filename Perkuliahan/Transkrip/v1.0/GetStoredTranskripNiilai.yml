info:
  name: GetStoredTranskripNiilai
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/perkuliahan/transkrip/:nim/stored"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: 23110139P
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
