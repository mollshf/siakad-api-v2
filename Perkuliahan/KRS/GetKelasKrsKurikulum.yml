info:
  name: GetKelasKrsKurikulum
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/perkuliahan/krs/kelas/:nim"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: "23310123"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
