info:
  name: GetKumpulanKrsYangDipilih
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/perkuliahan/krs/mahasiswa/:nim/koleksi"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: "22340002"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
