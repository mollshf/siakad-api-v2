info:
  name: GetKrs
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/perkuliahan/krs?exportTag=0"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: exportTag
      value: "0"
      type: query
    - name: id_periode
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
