info:
  name: GetNilaiTransferPendidikan
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/perkuliahan/nilai-transfer/:nim"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: 25183033P
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
