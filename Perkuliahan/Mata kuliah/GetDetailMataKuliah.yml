info:
  name: GetDetailMataKuliah
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/perkuliahan/mata-kuliah/:id"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: id
      value: 68b6354b2cc23532132fab37
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
