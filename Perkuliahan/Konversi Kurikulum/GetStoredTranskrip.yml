info:
  name: GetStoredTranskrip
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/perkuliahan/konversi/kurikulum/transkrip/:nim/stored"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: "19160046"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: API ini akan menampilkan data transkrip yang sudah difilter dengan unlist transkrip
