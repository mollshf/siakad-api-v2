info:
  name: GetHitungTranskrip
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/perkuliahan/konversi/kurikulum/transkrip/:nim?tahun=2023"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: tahun
      value: "2023"
      type: query
    - name: nim
      value: "19160046"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: API ini menampilkan data transkrip dari keseluruhan data yang ada pada system.
