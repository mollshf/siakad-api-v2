info:
  name: GetSesiPertemuanKelas
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/perkuliahan/attendance/session/?db_id_kelas_kuliah=689878098f4a606049ccfc54"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: db_id_kelas_kuliah
      value: 689878098f4a606049ccfc54
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
