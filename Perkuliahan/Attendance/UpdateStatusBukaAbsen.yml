info:
  name: UpdateStatusBukaAbsen
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/api/perkuliahan/attendance/session/:id/status"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
          "is_open": false // boolean
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
