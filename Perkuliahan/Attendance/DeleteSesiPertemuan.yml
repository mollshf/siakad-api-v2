info:
  name: DeleteSesiPertemuan
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/api/perkuliahan/attendance/session/:id"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
