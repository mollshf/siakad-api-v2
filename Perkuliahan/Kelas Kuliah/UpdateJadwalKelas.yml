info:
  name: UpdateJadwalKelas
  type: http
  seq: 7

http:
  method: PUT
  url: "{{url}}/api/perkuliahan/kelaskuliah/:_id/jadwal"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: _id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
          "hari": 7, // 1-7 = senin-minggu
          "jam_mulai": "10:10",
          "jam_selesai": "11:59",
          "id_ruangan": "RUANG MOIMOI"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
