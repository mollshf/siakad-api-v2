info:
  name: ResetPasswordDosen
  type: http
  seq: 13

http:
  method: POST
  url: "{{url}}/api/user/reset/password/dosen"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: "{\r

      \    \"id_dosen\": \"adc58bf8-c2f3-46a4-bd15-a3d9eb3ec0f1\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
