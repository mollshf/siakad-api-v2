info:
  name: RegisterDosen
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/api/user/lecture"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: "{\r

      \    \"nuptk\": \"4756779680230082\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
