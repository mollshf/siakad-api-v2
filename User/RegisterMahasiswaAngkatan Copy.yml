info:
  name: RegisterMahasiswaAngkatan Copy
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/user/mahasiswa/multiple"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: "{\r\n    \"angkatan\": 2023\r\n}\r\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
