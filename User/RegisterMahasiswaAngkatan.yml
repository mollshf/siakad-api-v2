info:
  name: RegisterMahasiswaAngkatan
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/user/mahasiswa/multiple"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: "{\r\n    \"angkatan\": 2025\r\n}\r\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
