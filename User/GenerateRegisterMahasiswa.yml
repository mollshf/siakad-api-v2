info:
  name: GenerateRegisterMahasiswa
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/user/mahasiswa/generate"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: "{\r

      \    \"periode\": \"20251\"\r

      }\r\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
