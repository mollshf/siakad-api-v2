info:
  name: RegisterMahasiswaAngkatan Copy_1
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/user/mahasiswa/multiple"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  body:
    type: json
    data: "{\r\n    \"angkatan\": 2022\r\n}\r\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
