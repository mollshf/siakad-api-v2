info:
  name: reject absen mahasiswa
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/api/absensi/buka-absen/reject"
  headers:
    - name: Authorization
      value: "{{accessTokenLocal}}"
  body:
    type: json
    data: "{\r

      \    \"id_absen_mhs\": \"65517a2bd2e3f702cec2e050\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
