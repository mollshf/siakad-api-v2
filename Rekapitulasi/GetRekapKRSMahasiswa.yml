info:
  name: GetRekapKRSMahasiswa
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/rekapitulasi/krs/mahasiswa/:nim"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
