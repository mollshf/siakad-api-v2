info:
  name: GetRekapKRSOnline
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/rekapitulasi/krs/online/:id_periode"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: id_periode
      value: "20251"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
