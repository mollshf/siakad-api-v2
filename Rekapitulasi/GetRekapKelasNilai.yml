info:
  name: GetRekapKelasNilai
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/rekapitulasi/kelas/nilai/:id_periode"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: id_periode
      value: "20251"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
