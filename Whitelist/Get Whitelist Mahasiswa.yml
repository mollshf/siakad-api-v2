info:
  name: Get Whitelist Mahasiswa
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/whitelist/:nim"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  params:
    - name: nim
      value: "2318100"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
