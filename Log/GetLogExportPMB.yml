info:
  name: GetLogExportPMB
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/log/export/pmb"
  headers:
    - name: Authorization
      value: "{{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
